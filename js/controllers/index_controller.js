/*
    posssible controller types depends on models:
        app.controller if nothing is returned
        app.object for single instances
        app.array for arrays
 */



/*App.IndexController = Ember.Controller.extend({
      for basic stuff like navigation descriptions and so on
        still empty since we have not a dynamic basic view yet ->
        would be for plain  ... </script> in index.html

});*/